{
  "name": "authentication-guard",
  "version": "1.0.0",
  "main": "dist/index.obf.js",
  "types": "dist/types/index.d.ts",
  "files": [
    "dist/",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "clean": "rimraf dist",
    "build:bundle": "webpack --config webpack.config.js",
  "build:types": "mkdir dist\\types && echo // Type declarations here > dist\\types\\index.d.ts",
    "build": "npm run clean && npm run build:bundle && npm run build:types"
  },
  "devDependencies": {
    "javascript-obfuscator": "^4.1.1",
    "rimraf": "^6.0.1",
    "webpack": "^5.0.0",
    "webpack-cli": "^4.0.0",
    "webpack-obfuscator": "^3.5.1"
  }
}
